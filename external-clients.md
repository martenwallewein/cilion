# Thoughts on using SCION for external (non-cluster) clients

- Somehow per node or cluster figure out SCION paths to client (e.g. in residential ISP) and add a path computation entry for these remotes. 